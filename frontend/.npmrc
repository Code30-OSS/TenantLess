# D-07 supply-chain gate: every install records an EXACT pin (no caret/tilde ranges).
# Combined with a committed package-lock.json + `npm ci`, this eliminates version drift
# across machines (RESEARCH Pitfall 8).
save-exact=true
